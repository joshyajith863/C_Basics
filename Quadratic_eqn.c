#include <stdio.h>
struct Quadratic_eqn
        {
            int coefficient;
            int exponent;
        }P1[10],P2[10],P3[10];

int main(){
       int constant;
        printf("Enter the coefficients of the Quadratic eqn :");
        scanf("%dx3+%dx2+%dx+c",&P1->coefficient);
        
        
}